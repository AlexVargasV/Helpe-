import 'package:flutter/material.dart';

class Galeria extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Center(
      child: Text('GALERIA', style: TextStyle(fontSize: 40.0),),
    );
  }
}